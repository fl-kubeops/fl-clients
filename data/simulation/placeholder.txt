This file is not a simulation input.

The `data/simulation/` directory is reserved for simulation data files.
If the directory is otherwise empty, `.gitkeep` is sufficient to retain it in version control.
